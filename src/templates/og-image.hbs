<!DOCTYPE html>
<html>
<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family={{fonts.heading}}:wght@{{fonts.headingWeight}}&family={{fonts.body}}:wght@{{fonts.bodyWeight}}&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      width: {{width}}px;
      height: {{height}}px;
      background: {{colors.background}};
      font-family: '{{fonts.body}}', sans-serif;
      color: {{colors.text}};
      overflow: hidden;
      position: relative;
    }

    /* Background gradient overlay */
    .bg-gradient {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: linear-gradient(
        {{style.gradientDirection}},
        {{colors.background}} 0%,
        {{colors.surface}} 50%,
        {{colors.background}} 100%
      );
      z-index: 0;
    }

    /* Accent glow */
    .glow {
      position: absolute;
      width: 400px;
      height: 400px;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.15;
      z-index: 1;
    }
    .glow-1 {
      top: -100px;
      right: -50px;
      background: {{colors.primary}};
    }
    .glow-2 {
      bottom: -100px;
      left: -50px;
      background: {{colors.accent}};
    }

    .container {
      position: relative;
      z-index: 2;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 80px;
      height: 100%;
    }

    .brand {
      font-family: '{{fonts.mono}}', monospace;
      font-size: 16px;
      color: {{colors.accent}};
      letter-spacing: 3px;
      text-transform: uppercase;
      margin-bottom: 24px;
    }

    .title {
      font-family: '{{fonts.heading}}', sans-serif;
      font-weight: {{fonts.headingWeight}};
      font-size: 56px;
      line-height: 1.15;
      color: {{colors.text}};
      margin-bottom: 20px;
      max-width: 800px;
    }

    .tagline {
      font-size: 22px;
      color: {{colors.textMuted}};
      line-height: 1.5;
      max-width: 600px;
    }

    .accent-line {
      width: 60px;
      height: 3px;
      background: linear-gradient(90deg, {{colors.primary}}, {{colors.accent}});
      margin: 24px 0;
      border-radius: 2px;
    }

    .cta {
      display: inline-block;
      margin-top: 32px;
      padding: 14px 32px;
      background: {{colors.primary}};
      color: {{colors.background}};
      font-family: '{{fonts.heading}}', sans-serif;
      font-weight: 600;
      font-size: 16px;
      border-radius: {{style.borderRadius}};
      letter-spacing: 0.5px;
    }
  </style>
</head>
<body>
  <div class="bg-gradient"></div>
  {{#if style.glowEffects}}
  <div class="glow glow-1"></div>
  <div class="glow glow-2"></div>
  {{/if}}

  <div class="container">
    <div class="brand">{{product.brand}}</div>
    <h1 class="title">{{product.product_name}}</h1>
    <div class="accent-line"></div>
    <p class="tagline">{{copy.og_description}}</p>
    <div class="cta">{{copy.call_to_action}}</div>
  </div>
</body>
</html>
